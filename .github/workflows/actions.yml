name: BearShell Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/pipeline/tests.yml  # This will reference the 'tests.yml' file
    with:
      python-version: '3.11'
  publish:
    uses: ./.github/workflows/pipeline/publish.yml  # This will reference the 'tests.yml' file
    with:
      python-version: '3.11'